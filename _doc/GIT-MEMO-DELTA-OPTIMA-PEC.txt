==========================================
GIT – MÉMO OFFICIEL DU PROJET DELTA OPTIMA
==========================================

CHEMIN DU PROJET :
E:\NOVELPIX\3. Activités\1. BE\Projets en cours\ABDOULAY\ALLIANZ\1. PROPOSITION CLIENT\0. HTML\0. CHATGPT\1. DELTA-OPTIMA-PEC

------------------------------------------
🎯 OBJECTIF
------------------------------------------
Mettre à jour GitHub après modification du code
(Antigravity, VS Code, éditeur de texte, etc.).

Toutes les modifications faites dans le dossier
sont prises en compte automatiquement par Git,
mais Git N’ENVOIE RIEN si tu ne lances pas
les commandes ci-dessous.

------------------------------------------
🔄 RITUEL POUR METTRE À JOUR GITHUB
------------------------------------------

1️⃣ Vérifier l’état actuel du projet
------------------------------------
git status

- Si tu vois "modified", "untracked" → il faut envoyer.
- Si tu vois "clean" → GitHub est déjà à jour.


2️⃣ Ajouter tous les fichiers modifiés
--------------------------------------
git add .


3️⃣ Créer le commit (version)
-----------------------------
git commit -m "MESSAGE"

Exemples :
git commit -m "Correction formulaire"
git commit -m "Mise à jour interface photos"
git commit -m "Amélioration PWA et service worker"


4️⃣ Envoyer sur GitHub
----------------------
git push


------------------------------------------
❗ COMMANDES À NE PLUS JAMAIS REFAIRE
------------------------------------------
git init
git branch -M main
git remote add origin https://github.com/Novelpix/tech-diagnostic-app.git

Ces commandes sont déjà en place définitivement.


------------------------------------------
📌 RAPPEL IMPORTANT
------------------------------------------
- Antigravity modifie les fichiers directement,
  MAIS Git ne les envoie pas tout seul.

- Le combo obligatoire est toujours :
  git add .
  git commit -m "MESSAGE"
  git push

- "git status" est ton radar principal :
  - "clean" = tout est synchronisé
  - "modified" = faut envoyer


------------------------------------------
Fin du mémo.
------------------------------------------
